[pytest]
# Tell pytest to look for code starting at the project root.
# This lets 'from app.main import create_app' work from the tests folder.
pythonpath = .

# Where to look for test files.
testpaths = tests

# Show concise but useful output.
addopts = -v --tb=short
